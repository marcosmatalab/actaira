// Inert stub. Seamark fixtures reconstruct the SHAPE of a configuration,
// never its effect. This file does nothing and is here so the reader has a
// real path to stat and digest.
// second copy, per the Wiz write-up: a setup.mjs in .claude as well as .vscode
